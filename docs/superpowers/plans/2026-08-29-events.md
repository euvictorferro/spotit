# Eventos Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Trocar `CarEvent.sample` por eventos reais criados por qualquer usuário, com RSVP ("Vou") persistido.

**Architecture:** Duas tabelas novas (`events`, `event_attendees`), RLS de leitura pública, escrita só pelo dono — **nenhuma das duas ganha policy de UPDATE** (padrão já consolidado nesta sessão depois do bug do DM). `SupabaseService` ganha 3 métodos, incluindo geocoding via `CLGeocoder` nativo no momento da criação. `EventsView` ganha um botão de criar; `EventDetailView` passa a receber `DBEvent`.

**Tech Stack:** SwiftUI, MapKit, CoreLocation (`CLGeocoder`), EventKit (já existia), supabase-swift, Supabase Postgres + RLS.

**Spec:** `docs/superpowers/specs/2026-08-29-events-design.md`

## Global Constraints

- Sem fonte externa de eventos, sem editar/cancelar evento, sem seletor de mapa customizado (geocoding de texto via `CLGeocoder`), sem eventos passados na lista (spec: Fora de escopo)
- `events`/`event_attendees`: NENHUMA policy de UPDATE em nenhuma das duas (spec: Dados)
- `lat`/`lng` nullable — geocoding é best-effort, evento existe mesmo sem coordenadas

---

### Task 1: Migration `events` + `event_attendees`

**Files:**
- Create: `supabase/migrations/0014_events.sql`

**Interfaces:**
- Produces: tabelas `events`, `event_attendees` com as colunas e as 5 policies exatas da spec (seção "Dados")

- [ ] **Step 1: Escrever a migration** com o SQL exato da spec, ao pé da letra (2 tabelas + 5 policies, nenhuma de UPDATE).
- [ ] **Step 2: NÃO tente `supabase db push`/`supabase link`** — sandbox bloqueia. Documentar, aplicação fica com o controller.
- [ ] **Step 3: Commit**

```bash
git add supabase/migrations/0014_events.sql
git commit -m "feat: tabelas events/event_attendees com RLS

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01M7VA3TbyjrvN1NFVSqxW8Q"
```

---

### Task 2: Modelo `DBEvent` + `SupabaseService`

**Files:**
- Create: `Spot It/Spot It/Models/DBEvent.swift`
- Modify: `Spot It/Spot It/Services/SupabaseService.swift`

**Interfaces:**
- Produces:
  - `struct DBEvent: Identifiable { let id: UUID; let organizerId: UUID; let organizerUsername: String; let name: String; let location: String; let lat: Double?; let lng: Double?; let eventDate: Date; let description: String?; let attendeeCount: Int; var isGoing: Bool }`
  - `static func createEvent(name: String, location: String, eventDate: Date, description: String?) async throws`
  - `static func fetchEvents() async throws -> [DBEvent]`
  - `static func toggleGoing(eventId: UUID) async throws -> Bool`

- [ ] **Step 1: Escrever `DBEvent.swift`** dentro de `Spot It/Spot It/Models/` (pasta sincronizada — confirme com `find` depois de criar):

```swift
import Foundation

struct DBEvent: Identifiable {
    let id: UUID
    let organizerId: UUID
    let organizerUsername: String
    let name: String
    let location: String
    let lat: Double?
    let lng: Double?
    let eventDate: Date
    let description: String?
    let attendeeCount: Int
    var isGoing: Bool
}
```

- [ ] **Step 2: `createEvent` com geocoding.**

```swift
    static func createEvent(name: String, location: String, eventDate: Date, description: String?) async throws {
        try ensureSignedIn()
        guard let myId = client.auth.currentSession?.user.id else { throw SupabaseError.notSignedIn }

        var lat: Double?
        var lng: Double?
        if let placemark = try? await CLGeocoder().geocodeAddressString(location).first,
           let coordinate = placemark.location?.coordinate {
            lat = coordinate.latitude
            lng = coordinate.longitude
        }

        struct NewEvent: Encodable {
            let organizer_id: UUID
            let name: String
            let location: String
            let lat: Double?
            let lng: Double?
            let event_date: Date
            let description: String?
        }
        try await client.from("events")
            .insert(NewEvent(organizer_id: myId, name: name, location: location, lat: lat, lng: lng, event_date: eventDate, description: description))
            .execute()
    }
```

(precisa `import CoreLocation` no topo do arquivo se ainda não tiver)

- [ ] **Step 3: `fetchEvents`** — eventos futuros, com contagem de confirmados e se o usuário atual confirmou (mesmo padrão N+1 já usado em `fetchFeedPosts`/`fetchConversations`):

```swift
    static func fetchEvents() async throws -> [DBEvent] {
        try ensureSignedIn()
        let myId = client.auth.currentSession?.user.id

        struct EventRow: Decodable {
            let id: UUID
            let organizer_id: UUID
            let name: String
            let location: String
            let lat: Double?
            let lng: Double?
            let event_date: Date
            let description: String?
        }

        let rows: [EventRow] = try await client.from("events")
            .select()
            .gte("event_date", value: Date())
            .order("event_date", ascending: true)
            .execute()
            .value

        var events: [DBEvent] = []
        for row in rows {
            struct ProfileRow: Decodable { let username: String }
            let organizerUsername = (try? await client.from("profiles")
                .select("username")
                .eq("id", value: row.organizer_id)
                .single()
                .execute()
                .value as ProfileRow)?.username ?? "usuário"

            let attendeeCount: Int = (try? await client.from("event_attendees")
                .select("event_id", head: true, count: .exact)
                .eq("event_id", value: row.id)
                .execute()
                .count) ?? 0

            var isGoing = false
            if let myId {
                struct AttendeeRow: Decodable { let user_id: UUID }
                let mine: [AttendeeRow] = (try? await client.from("event_attendees")
                    .select("user_id")
                    .eq("event_id", value: row.id)
                    .eq("user_id", value: myId)
                    .limit(1)
                    .execute()
                    .value) ?? []
                isGoing = !mine.isEmpty
            }

            events.append(DBEvent(
                id: row.id, organizerId: row.organizer_id, organizerUsername: organizerUsername,
                name: row.name, location: row.location, lat: row.lat, lng: row.lng,
                eventDate: row.event_date, description: row.description,
                attendeeCount: attendeeCount, isGoing: isGoing
            ))
        }
        return events
    }
```

- [ ] **Step 4: `toggleGoing`** (mesmo padrão de `toggleLike`, engolindo duplicata):

```swift
    static func toggleGoing(eventId: UUID) async throws -> Bool {
        try ensureSignedIn()
        guard let myId = client.auth.currentSession?.user.id else { throw SupabaseError.notSignedIn }

        struct AttendeeRow: Decodable { let user_id: UUID }
        let existing: [AttendeeRow] = try await client.from("event_attendees")
            .select("user_id")
            .eq("event_id", value: eventId)
            .eq("user_id", value: myId)
            .limit(1)
            .execute()
            .value

        if existing.isEmpty {
            struct NewAttendee: Encodable { let event_id: UUID; let user_id: UUID }
            do {
                try await client.from("event_attendees")
                    .insert(NewAttendee(event_id: eventId, user_id: myId))
                    .execute()
            } catch {
                if !error.localizedDescription.contains("duplicate key") { throw error }
            }
            return true
        } else {
            try await client.from("event_attendees")
                .delete()
                .eq("event_id", value: eventId)
                .eq("user_id", value: myId)
                .execute()
            return false
        }
    }
```

- [ ] **Step 5: Build** — `xcodebuild -project "Spot It/Spot It.xcodeproj" -scheme "Spotted" -destination "generic/platform=iOS Simulator" build` → `** BUILD SUCCEEDED **`.
- [ ] **Step 6: Commit**

```bash
git add "Spot It/Spot It/Models/DBEvent.swift" "Spot It/Spot It/Services/SupabaseService.swift"
git commit -m "feat: modelo DBEvent + métodos de eventos no SupabaseService

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01M7VA3TbyjrvN1NFVSqxW8Q"
```

---

### Task 3: `EventsView` com dado real + criar evento

**Files:**
- Modify: `Spot It/Spot It/Views/EventsView.swift`

**Interfaces:**
- Consumes: `fetchEvents()`, `createEvent(...)`, `toggleGoing(eventId:)` (Task 2), `DBEvent` (Task 2)

- [ ] **Step 1: Ler o arquivo inteiro.** Trocar `private let events: [CarEvent] = []` e `@State private var going: Set<UUID> = []` por `@State private var events: [DBEvent] = []`, adicionar `.task { await load() }` + `.refreshable { await load() }`:

```swift
    private func load() async {
        events = (try? await SupabaseService.fetchEvents()) ?? []
    }
```

- [ ] **Step 2: Adaptar `card(_:)`.** `event.attendees` → `event.attendeeCount`, `going.contains(event.id)` → `event.isGoing`, `event.dateLabel` (string pronta) → formate `event.eventDate` com um `DateFormatter`/`.formatted()` simples (não precisa replicar o texto exato dos samples, só uma data legível tipo "sáb, 30 de agosto · 08:00"). `event.coverGradient` não existe mais em `DBEvent` — derive um gradiente determinístico a partir de `event.id`/`event.name` (mesma técnica de hash já usada em `SearchableUser` pra cor de avatar, adaptada pra 2 cores de fundo).
- [ ] **Step 3: Botão "Vou" chama `toggleGoing` real** — async, atualiza `events[index].isGoing`/`attendeeCount` local com o retorno.
- [ ] **Step 4: Botão "+" na toolbar** abre um sheet de criação:

```swift
private struct CreateEventSheet: View {
    let onCreated: () -> Void
    @Environment(\.dismiss) private var dismiss
    @State private var name = ""
    @State private var location = ""
    @State private var eventDate = Date()
    @State private var description = ""
    @State private var isCreating = false
    @State private var errorMessage: String?

    var body: some View {
        NavigationStack {
            Form {
                Section("Nome") { TextField("Ex: Cars & Coffee Naples", text: $name) }
                Section("Local") { TextField("Ex: Naples, FL", text: $location) }
                Section("Data") { DatePicker("", selection: $eventDate).labelsHidden() }
                Section("Descrição (opcional)") { TextField("Conta mais sobre o evento...", text: $description, axis: .vertical) }
                if let errorMessage {
                    Text(errorMessage).foregroundStyle(.red)
                }
            }
            .navigationTitle("Novo Evento")
            .navigationBarTitleDisplayMode(.inline)
            .toolbar {
                ToolbarItem(placement: .cancellationAction) {
                    Button("Cancelar") { dismiss() }
                }
                ToolbarItem(placement: .confirmationAction) {
                    Button("Criar") { Task { await create() } }
                        .disabled(name.isEmpty || location.isEmpty || isCreating)
                }
            }
        }
    }

    private func create() async {
        isCreating = true
        defer { isCreating = false }
        do {
            try await SupabaseService.createEvent(
                name: name, location: location, eventDate: eventDate,
                description: description.isEmpty ? nil : description
            )
            dismiss()
            onCreated()
        } catch {
            errorMessage = error.localizedDescription
        }
    }
}
```

Adicionar `@State private var showCreate = false` em `EventsView`, um `ToolbarItem(placement: .topBarTrailing)` com ícone `"plus"` que abre `.sheet(isPresented: $showCreate) { CreateEventSheet(onCreated: { Task { await load() } }) }`.

- [ ] **Step 5: `binding(for:)` antigo não é mais necessário** (usado pelo `EventDetailView` com `@Binding var isGoing: Bool` — a Task 4 muda essa assinatura). Se sobrar código morto depois desta task por causa disso, tudo bem, a Task 4 termina de limpar.
- [ ] **Step 6: Build.** Pode sobrar erro em `EventDetailView.swift` (Task 4 ainda não rodou) — se for só lá, documente e prossiga; qualquer outro erro precisa ser corrigido.
- [ ] **Step 7: Commit**

```bash
git add "Spot It/Spot It/Views/EventsView.swift"
git commit -m "feat: EventsView usa eventos reais + criar evento

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01M7VA3TbyjrvN1NFVSqxW8Q"
```

---

### Task 4: `EventDetailView` com `DBEvent`

**Files:**
- Modify: `Spot It/Spot It/Views/EventDetailView.swift`

**Interfaces:**
- Consumes: `DBEvent` (Task 2), `toggleGoing(eventId:)` (Task 2)

- [ ] **Step 1: Ler o arquivo inteiro.** Trocar `let event: CarEvent` por `let event: DBEvent` (ou `@State private var event: DBEvent` se for mais fácil atualizar `isGoing`/`attendeeCount` localmente após o toggle — escolha o que exigir menos mudança estrutural). Remover `@Binding var isGoing: Bool` — o estado de "vou" agora vem de `event.isGoing` e é gravado via `SupabaseService.toggleGoing(eventId:)`.
- [ ] **Step 2: Mapa condicional.** `position`/`Map` só renderizam se `event.lat`/`event.lng` não forem nil — monte um `CLLocationCoordinate2D` a partir deles quando existirem; se nil, omita o bloco do `Map` inteiro (o resto da tela continua normal, só sem o mapa).
- [ ] **Step 3: Botão "Vou"** chama `toggleGoing` real igual ao padrão já usado em outras telas desta sessão (curtir, seguir).
- [ ] **Step 4: `addToCalendar()`** usa `event.eventDate` pro `startDate` e `event.eventDate.addingTimeInterval(3 * 3600)` pro `endDate` (não existe `endDate` real em `DBEvent` — 3h é uma duração razoável default, mesma usada nos samples antigos).
- [ ] **Step 5: `infoRow` de organizador/confirmados** usa `event.organizerUsername`/`event.attendeeCount`.
- [ ] **Step 6: Ajustar o call site em `EventsView.swift`** (Task 3) — confirme que passa `event: DBEvent` sem `isGoing` binding solto.
- [ ] **Step 7: Build.** `xcodebuild -project "Spot It/Spot It.xcodeproj" -scheme "Spotted" -destination "generic/platform=iOS Simulator" build` → **agora sim** `** BUILD SUCCEEDED **` sem erro nenhum em lugar nenhum do projeto (última task da branch). Depois disso, `Spot It/Spot It/Models/CarEvent.swift` provavelmente fica órfão — rode `grep -rn "CarEvent\b" "Spot It/Spot It"` e, se não sobrar nenhuma referência fora de `#Preview`s já ajustados, delete o arquivo.
- [ ] **Step 8: Commit**

```bash
git add "Spot It/Spot It/Views/EventDetailView.swift" "Spot It/Spot It/Views/EventsView.swift"
git rm --ignore-unmatch "Spot It/Spot It/Models/CarEvent.swift"
git commit -m "feat: EventDetailView usa DBEvent real

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01M7VA3TbyjrvN1NFVSqxW8Q"
```
