# DM (mensagens diretas) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Trocar o DM em memória (`DMConversation.sample`) por conversas e mensagens reais persistidas no Supabase, sem mudar o visual já existente.

**Architecture:** Duas tabelas novas (`conversations`, `messages`) com RLS por participante. `SupabaseService` ganha 4 métodos. `DMView`/`ChatThreadView`/`UserProfileView` passam a carregar/gravar dado real em vez de array vazio/mock.

**Tech Stack:** SwiftUI, supabase-swift, Supabase Postgres + RLS.

**Spec:** `docs/superpowers/specs/2026-08-29-dm-design.md`

## Global Constraints

- Sem realtime/push — só carrega ao abrir/dar refresh (spec: Fora de escopo)
- Um único par (`user_a`, `user_b`) só pode ter UMA conversa — sempre com `user_a < user_b` (spec: Dados)
- RLS: só participante lê/escreve sua própria conversa/mensagens (spec: RLS)
- Mensagens são imutáveis — sem update/delete (spec: RLS)

---

### Task 1: Migration `conversations` + `messages`

**Files:**
- Create: `supabase/migrations/0005_dm.sql`

**Interfaces:**
- Produces: tabelas `conversations(id, user_a, user_b, last_message_at, created_at)` e `messages(id, conversation_id, sender_id, text, created_at)`, com as 4 policies do spec (RLS)

- [ ] **Step 1: Escrever a migration** com o SQL exato da seção "Dados" e "RLS" da spec (`docs/superpowers/specs/2026-08-29-dm-design.md`) — copiar as duas tabelas + índice único + índice de mensagens + as 4 policies, ao pé da letra.

- [ ] **Step 2: Aplicar** — `cd "Spot It" && supabase db push` (ou SQL editor do projeto `mevdvmjtkkcerkakzkch` se o CLI não estiver linkado).

- [ ] **Step 3: Verificar** — `select * from conversations; select * from messages;` no SQL editor, ambas vazias, sem erro.

- [ ] **Step 4: Commit**

```bash
git add supabase/migrations/0005_dm.sql
git commit -m "feat: tabelas conversations/messages com RLS por participante

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01M7VA3TbyjrvN1NFVSqxW8Q"
```

---

### Task 2: Modelos `ConversationSummary` e `DBMessage`

**Files:**
- Create: `Spot It/Spot It/Models/ConversationSummary.swift`
- Create: `Spot It/Spot It/Models/DBMessage.swift`

**Interfaces:**
- Produces:
  - `struct ConversationSummary: Decodable, Identifiable { let id: UUID; let otherUserId: UUID; let otherUsername: String; let otherAvatarUrl: String?; let lastMessageText: String?; let lastMessageAt: Date }`
  - `struct DBMessage: Decodable, Identifiable { let id: UUID; let conversationId: UUID; let senderId: UUID; let text: String; let createdAt: Date }`

- [ ] **Step 1: Escrever `ConversationSummary.swift`**

```swift
import Foundation

/// Uma conversa da lista do DM, já com o outro participante resolvido —
/// o backend faz esse join (ver SupabaseService.fetchConversations).
struct ConversationSummary: Decodable, Identifiable {
    let id: UUID
    let otherUserId: UUID
    let otherUsername: String
    let otherAvatarUrl: String?
    let lastMessageText: String?
    let lastMessageAt: Date
}
```

(Não precisa de `CodingKeys` — esse struct não é decodificado direto de uma
linha do Postgrest, é montado manualmente em `SupabaseService` a partir de
múltiplas queries; ver Task 3.)

- [ ] **Step 2: Escrever `DBMessage.swift`**

```swift
import Foundation

struct DBMessage: Decodable, Identifiable, Equatable {
    let id: UUID
    let conversationId: UUID
    let senderId: UUID
    let text: String
    let createdAt: Date

    enum CodingKeys: String, CodingKey {
        case id, text
        case conversationId = "conversation_id"
        case senderId = "sender_id"
        case createdAt = "created_at"
    }
}
```

- [ ] **Step 3: Build**

`xcodebuild -project "Spot It/Spot It.xcodeproj" -scheme "Spotted" -destination "generic/platform=iOS Simulator" build` → `** BUILD SUCCEEDED **`. Confirmar os dois arquivos foram criados dentro de `Spot It/Spot It/Models/` (pasta sincronizada com o target — não em `Spot It/Models/`).

- [ ] **Step 4: Commit**

```bash
git add "Spot It/Spot It/Models/ConversationSummary.swift" "Spot It/Spot It/Models/DBMessage.swift"
git commit -m "feat: modelos ConversationSummary e DBMessage

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01M7VA3TbyjrvN1NFVSqxW8Q"
```

---

### Task 3: `SupabaseService` — 4 métodos de DM

**Files:**
- Modify: `Spot It/Spot It/Services/SupabaseService.swift`

**Interfaces:**
- Consumes: `ConversationSummary`, `DBMessage` (Task 2), `ensureSignedIn()` (já existe), `client` (já existe)
- Produces:
  - `static func fetchConversations() async throws -> [ConversationSummary]`
  - `static func fetchMessages(conversationId: UUID) async throws -> [DBMessage]`
  - `static func sendMessage(conversationId: UUID, text: String) async throws`
  - `static func startOrFetchConversation(withUserId otherId: UUID) async throws -> UUID`

- [ ] **Step 1: Adicionar os 4 métodos** ao final do `struct SupabaseService` (antes do `}` final), todos chamando `try ensureSignedIn()` primeiro:

```swift
    static func startOrFetchConversation(withUserId otherId: UUID) async throws -> UUID {
        try ensureSignedIn()
        guard let myId = client.auth.currentSession?.user.id else { throw SupabaseError.notSignedIn }

        let userA = min(myId, otherId)
        let userB = max(myId, otherId)

        struct ConversationId: Decodable { let id: UUID }

        if let existing: ConversationId = try? await client.from("conversations")
            .select("id")
            .eq("user_a", value: userA)
            .eq("user_b", value: userB)
            .single()
            .execute()
            .value {
            return existing.id
        }

        struct NewConversation: Encodable {
            let user_a: UUID
            let user_b: UUID
        }
        let inserted: ConversationId = try await client.from("conversations")
            .insert(NewConversation(user_a: userA, user_b: userB))
            .select("id")
            .single()
            .execute()
            .value
        return inserted.id
    }

    static func fetchConversations() async throws -> [ConversationSummary] {
        try ensureSignedIn()
        guard let myId = client.auth.currentSession?.user.id else { throw SupabaseError.notSignedIn }

        struct ConversationRow: Decodable {
            let id: UUID
            let user_a: UUID
            let user_b: UUID
            let last_message_at: Date
        }

        let rows: [ConversationRow] = try await client.from("conversations")
            .select()
            .or("user_a.eq.\(myId),user_b.eq.\(myId)")
            .order("last_message_at", ascending: false)
            .execute()
            .value

        var summaries: [ConversationSummary] = []
        for row in rows {
            let otherId = row.user_a == myId ? row.user_b : row.user_a

            struct ProfileRow: Decodable {
                let username: String
                let avatar_url: String?
            }
            guard let profile: ProfileRow = try? await client.from("profiles")
                .select("username, avatar_url")
                .eq("id", value: otherId)
                .single()
                .execute()
                .value
            else { continue }

            struct LastMessageRow: Decodable { let text: String }
            let lastMessage: LastMessageRow? = try? await client.from("messages")
                .select("text")
                .eq("conversation_id", value: row.id)
                .order("created_at", ascending: false)
                .limit(1)
                .single()
                .execute()
                .value

            summaries.append(ConversationSummary(
                id: row.id, otherUserId: otherId, otherUsername: profile.username,
                otherAvatarUrl: profile.avatar_url, lastMessageText: lastMessage?.text,
                lastMessageAt: row.last_message_at
            ))
        }
        return summaries
    }

    static func fetchMessages(conversationId: UUID) async throws -> [DBMessage] {
        try ensureSignedIn()
        return try await client.from("messages")
            .select()
            .eq("conversation_id", value: conversationId)
            .order("created_at", ascending: true)
            .execute()
            .value
    }

    static func sendMessage(conversationId: UUID, text: String) async throws {
        try ensureSignedIn()
        guard let myId = client.auth.currentSession?.user.id else { throw SupabaseError.notSignedIn }

        struct NewMessage: Encodable {
            let conversation_id: UUID
            let sender_id: UUID
            let text: String
        }
        try await client.from("messages")
            .insert(NewMessage(conversation_id: conversationId, sender_id: myId, text: text))
            .execute()

        struct TouchConversation: Encodable { let last_message_at: Date }
        try await client.from("conversations")
            .update(TouchConversation(last_message_at: Date()))
            .eq("id", value: conversationId)
            .execute()
    }
```

Nota pro implementador: se o supabase-swift instalado não aceitar exatamente esses métodos encadeados (`.or(...)`, `.single()` em insert com `.select()`), ajuste pra API real do SDK — confirme a assinatura antes de assumir (mesma lição já registrada no preflight do plano de Auth). O comportamento (não a sintaxe exata) é o que importa: buscar conversas do usuário logado com o outro participante resolvido, mensagens ordenadas, inserir mensagem + atualizar `last_message_at`, buscar-ou-criar conversa por par ordenado de ids.

- [ ] **Step 2: Build**

`xcodebuild -project "Spot It/Spot It.xcodeproj" -scheme "Spotted" -destination "generic/platform=iOS Simulator" build` → `** BUILD SUCCEEDED **`, confirmar `SupabaseService.swift` recompilado.

- [ ] **Step 3: Commit**

```bash
git add "Spot It/Spot It/Services/SupabaseService.swift"
git commit -m "feat: SupabaseService ganha métodos de DM (conversas/mensagens)

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01M7VA3TbyjrvN1NFVSqxW8Q"
```

---

### Task 4: `DMView` + `ChatThreadView` com dado real

**Files:**
- Modify: `Spot It/Spot It/Views/DMView.swift`

**Interfaces:**
- Consumes: `SupabaseService.fetchConversations()`, `fetchMessages(conversationId:)`, `sendMessage(conversationId:text:)` (Task 3), `ConversationSummary`, `DBMessage` (Task 2)

- [ ] **Step 1: `DMView` carrega conversas reais.** Adicionar `@State private var summaries: [ConversationSummary] = []`, `@State private var isLoading = true`, um `.task { await load() }` + `.refreshable { await load() }`, e uma função:

```swift
    private func load() async {
        isLoading = true
        summaries = (try? await SupabaseService.fetchConversations()) ?? []
        isLoading = false
    }
```

Trocar a fonte da `List` (hoje `$conversations.filter { ... }`) pra iterar `summaries` filtradas por `otherUsername`, e adaptar `row(_:)` pra receber um `ConversationSummary` (usar `otherUsername` no lugar de `username`, `lastMessageText ?? ""` no lugar de `lastMessage`, formatar `lastMessageAt` num "tempo atrás" simples — pode reusar/adaptar qualquer helper de data já existente no projeto, ou formatar inline com `RelativeDateTimeFormatter`). Remover o `@State private var conversations: [DMConversation]` antigo e o `DMConversation`/`ChatMessage` em memória deixam de ser a fonte de verdade da lista (o tipo em si pode continuar existindo só se `ChatThreadView` ainda precisar dele — ver Step 2).

- [ ] **Step 2: `ChatThreadView` carrega/envia mensagens reais.** Trocar a assinatura de `ChatThreadView` pra receber o necessário pra buscar mensagens de verdade — recomendado: `let conversationId: UUID`, `let otherUsername: String`, `let otherAvatarUrl: String?` no lugar do `@Binding var conversation: DMConversation` atual. Dentro:

```swift
    @State private var messages: [DBMessage] = []
    @State private var myUserId: UUID?
```

- `.task` busca `fetchMessages(conversationId:)` e guarda `myUserId = SupabaseService.client.auth.currentSession?.user.id` (pra decidir bolha própria vs do outro — troca a checagem `message.fromMe` por `message.senderId == myUserId`)
- `send()` vira `async`, chama `SupabaseService.sendMessage(conversationId:text:)`, depois recarrega `messages` (recarga simples é aceitável — sem otimista, sem realtime, conforme spec)
- O `NavigationLink` que abre `ChatThreadView` (dentro de `row`) passa `conversationId: conversation.id, otherUsername: conversation.otherUsername, otherAvatarUrl: conversation.otherAvatarUrl`

Manter o visual das bolhas (`BubbleShape`) e o resto do layout exatamente como está — só a fonte de dado muda.

- [ ] **Step 3: Build**

`xcodebuild -project "Spot It/Spot It.xcodeproj" -scheme "Spotted" -destination "generic/platform=iOS Simulator" build` → `** BUILD SUCCEEDED **`.

- [ ] **Step 4: Commit**

```bash
git add "Spot It/Spot It/Views/DMView.swift"
git commit -m "feat: DMView e ChatThreadView usam conversas/mensagens reais

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01M7VA3TbyjrvN1NFVSqxW8Q"
```

---

### Task 5: `UserProfileView` inicia conversa real

**Files:**
- Modify: `Spot It/Spot It/Views/UserProfileView.swift`
- Modify: qualquer call site de `UserProfileView(username:avatarInitials:avatarColors:)` que precise passar o `userId` real (buscar com `grep -rn "UserProfileView(" "Spot It/Spot It"` — no momento desta task o único conhecido é o `#Preview` do próprio arquivo, que pode usar um UUID fixo qualquer; se houver outros, ajustar o mínimo pra compilar)

**Interfaces:**
- Consumes: `SupabaseService.startOrFetchConversation(withUserId:)` (Task 3)

- [ ] **Step 1: `UserProfileView` ganha `let userId: UUID`** no seu `init`, ao lado de `username`/`avatarInitials`/`avatarColors`.

- [ ] **Step 2: Botão "Mensagem" busca/cria a conversa antes de navegar.** Trocar o `NavigationLink(destination: ChatThreadView(conversation: $conversation))` atual por um `Button` que, ao tocar, chama `startOrFetchConversation(withUserId: userId)` async, guarda o `conversationId` retornado num `@State private var openConversationId: UUID?`, e usa `.navigationDestination(item: $openConversationId)` (ou `NavigationLink` com `isActive`/valor, o padrão que o projeto já usa em outras telas) pra abrir `ChatThreadView(conversationId: id, otherUsername: username, otherAvatarUrl: nil)` (Task 4 já definiu essa assinatura). Erro ao criar conversa: mostrar inline, mesmo padrão de `errorMessage` já usado em `AuthView`/`OnboardingView`.

- [ ] **Step 3: Remover** o `@State private var conversation: DMConversation` antigo e a inicialização dele no `init` — não é mais necessário.

- [ ] **Step 4: Build**

`xcodebuild -project "Spot It/Spot It.xcodeproj" -scheme "Spotted" -destination "generic/platform=iOS Simulator" build` → `** BUILD SUCCEEDED **`. Se algum outro arquivo que chama `UserProfileView(...)` quebrar por faltar `userId`, ajustar esse call site passando o id disponível no contexto (ou um valor óbvio de placeholder documentado com `// ponytail:` se o dado real ainda não existir ali — não é escopo desta task resolver identidade de usuário em outras telas que não sejam DM).

- [ ] **Step 5: Commit**

```bash
git add "Spot It/Spot It/Views/UserProfileView.swift"
git commit -m "feat: UserProfileView inicia conversa real ao tocar Mensagem

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01M7VA3TbyjrvN1NFVSqxW8Q"
```
