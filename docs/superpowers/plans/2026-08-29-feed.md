# Feed Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Trocar o Feed em memória (`FeedPost.sample`/`Comment.sampleFor`) por posts, curtidas e comentários reais persistidos no Supabase, com um jeito de publicar um item já salvo na Wallet.

**Architecture:** Três tabelas novas (`posts`, `likes`, `comments`), todas com RLS de leitura pública e escrita só pelo dono, sem policy de UPDATE em nenhuma (lição do bug de segurança do DM). `posts` denormaliza os dados do carro na hora da publicação — nunca faz join com `wallet_items` de outro usuário. `SupabaseService` ganha 5 métodos. `FeedView`/`FeedPostCard`/`CommentsSheet` passam a usar dado real; `CarDetailPageView` ganha um botão opcional de publicar.

**Tech Stack:** SwiftUI, supabase-swift, Supabase Postgres + RLS.

**Spec:** `docs/superpowers/specs/2026-08-29-feed-design.md`

## Global Constraints

- Sem localização legível, sem "curtido por fulano", sem editar/deletar post/comentário, sem feed personalizado por "seguindo", sem notificação de curtida/comentário (spec: Fora de escopo)
- Nenhuma tabela nova ganha policy de RLS de UPDATE sem `WITH CHECK` explícito — de preferência nenhuma policy de UPDATE, seguindo o padrão de `follows`/`messages` (lição do bug de segurança encontrado na revisão final do DM)
- `posts` nunca faz join/leitura de `wallet_items` de outro usuário — dados denormalizados no insert (spec: Dados → posts)

---

### Task 1: Migration `posts` + `likes` + `comments`

**Files:**
- Create: `supabase/migrations/0010_feed.sql`

**Interfaces:**
- Produces: tabelas `posts`, `likes`, `comments` com as colunas e as 7 policies exatas da seção "Dados" da spec

- [ ] **Step 1: Escrever a migration** com o SQL exato das 3 tabelas + índice de `comments` + todas as policies da spec (seções "Dados" → `posts`/`likes`/`comments`), ao pé da letra.
- [ ] **Step 2: NÃO tente `supabase db push`/`supabase link`** — o sandbox bloqueia CLI/rede do Supabase. Documente isso no relatório; a aplicação em produção fica com o controller via Supabase Management API.
- [ ] **Step 3: Commit**

```bash
git add supabase/migrations/0010_feed.sql
git commit -m "feat: tabelas posts/likes/comments com RLS

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01M7VA3TbyjrvN1NFVSqxW8Q"
```

---

### Task 2: Modelos `DBPost` e `DBComment`

**Files:**
- Create: `Spot It/Spot It/Models/DBPost.swift`
- Create: `Spot It/Spot It/Models/DBComment.swift`

**Interfaces:**
- Produces:
  - `struct DBPost: Identifiable { let id: UUID; let userId: UUID; let username: String; let avatarUrl: String?; let modelo: String; let raridade: Int; let valorEstimadoUsd: Double; let fotoUrl: String; let caption: String?; let createdAt: Date; let likeCount: Int; let commentCount: Int; var likedByMe: Bool }`
  - `struct DBComment: Identifiable, Decodable { let id: UUID; let postId: UUID; let userId: UUID; let username: String; let text: String; let createdAt: Date }`

- [ ] **Step 1: Escrever `DBPost.swift`.** Não precisa ser `Decodable` puro — é montado manualmente em `SupabaseService.fetchFeedPosts()` a partir de múltiplas queries (post + like/comment counts + profile), igual ao padrão já usado em `ConversationSummary`:

```swift
import Foundation

struct DBPost: Identifiable {
    let id: UUID
    let userId: UUID
    let username: String
    let avatarUrl: String?
    let modelo: String
    let raridade: Int
    let valorEstimadoUsd: Double
    let fotoUrl: String
    let caption: String?
    let createdAt: Date
    let likeCount: Int
    let commentCount: Int
    var likedByMe: Bool
}
```

- [ ] **Step 2: Escrever `DBComment.swift`.** Este É decodificado direto de uma query com join simples (post_id + user_id + profiles.username):

```swift
import Foundation

struct DBComment: Identifiable {
    let id: UUID
    let postId: UUID
    let userId: UUID
    let username: String
    let text: String
    let createdAt: Date
}
```

(monte manualmente também, igual `DBPost` — mais simples que lutar contra o formato de join do PostgREST; ver Task 3)

- [ ] **Step 3: Build.** `xcodebuild -project "Spot It/Spot It.xcodeproj" -scheme "Spotted" -destination "generic/platform=iOS Simulator" build` → `** BUILD SUCCEEDED **`. Confirme os 2 arquivos dentro de `Spot It/Spot It/Models/` (pasta sincronizada com o target — NÃO `Spot It/Models/`, um nível acima; isso já causou bug em tasks anteriores desta mesma sessão, confira com atenção redobrada).
- [ ] **Step 4: Commit**

```bash
git add "Spot It/Spot It/Models/DBPost.swift" "Spot It/Spot It/Models/DBComment.swift"
git commit -m "feat: modelos DBPost e DBComment

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01M7VA3TbyjrvN1NFVSqxW8Q"
```

---

### Task 3: `SupabaseService` — 5 métodos de Feed

**Files:**
- Modify: `Spot It/Spot It/Services/SupabaseService.swift`

**Interfaces:**
- Consumes: `DBPost`, `DBComment` (Task 2)
- Produces:
  - `static func createPost(walletItemId: UUID?, modelo: String, raridade: Int, valorEstimadoUsd: Double, fotoUrl: String, caption: String?) async throws`
  - `static func fetchFeedPosts() async throws -> [DBPost]`
  - `static func toggleLike(postId: UUID) async throws -> Bool`
  - `static func fetchComments(postId: UUID) async throws -> [DBComment]`
  - `static func addComment(postId: UUID, text: String) async throws`

- [ ] **Step 1: `createPost`**

```swift
    static func createPost(walletItemId: UUID?, modelo: String, raridade: Int, valorEstimadoUsd: Double, fotoUrl: String, caption: String?) async throws {
        try ensureSignedIn()
        guard let myId = client.auth.currentSession?.user.id else { throw SupabaseError.notSignedIn }
        struct NewPost: Encodable {
            let user_id: UUID
            let wallet_item_id: UUID?
            let modelo: String
            let raridade: Int
            let valor_estimado_usd: Double
            let foto_url: String
            let caption: String?
        }
        try await client.from("posts")
            .insert(NewPost(user_id: myId, wallet_item_id: walletItemId, modelo: modelo, raridade: raridade, valor_estimado_usd: valorEstimadoUsd, foto_url: fotoUrl, caption: caption))
            .execute()
    }
```

- [ ] **Step 2: `fetchFeedPosts`** — busca posts, depois pra cada um resolve autor (`profiles`), contagem de curtidas, contagem de comentários, e se o usuário atual curtiu. N+1 é aceitável (mesmo padrão já aceito em `fetchConversations` desta sessão) — não otimize prematuramente:

```swift
    static func fetchFeedPosts() async throws -> [DBPost] {
        try ensureSignedIn()
        let myId = client.auth.currentSession?.user.id

        struct PostRow: Decodable {
            let id: UUID
            let user_id: UUID
            let modelo: String
            let raridade: Int
            let valor_estimado_usd: Double
            let foto_url: String
            let caption: String?
            let created_at: Date
        }

        let rows: [PostRow] = try await client.from("posts")
            .select()
            .order("created_at", ascending: false)
            .limit(50)
            .execute()
            .value

        var posts: [DBPost] = []
        for row in rows {
            struct ProfileRow: Decodable { let username: String; let avatar_url: String? }
            guard let profile: ProfileRow = try? await client.from("profiles")
                .select("username, avatar_url")
                .eq("id", value: row.user_id)
                .single()
                .execute()
                .value
            else { continue }

            let likeCount: Int = (try? await client.from("likes")
                .select("post_id", head: true, count: .exact)
                .eq("post_id", value: row.id)
                .execute()
                .count) ?? 0

            let commentCount: Int = (try? await client.from("comments")
                .select("id", head: true, count: .exact)
                .eq("post_id", value: row.id)
                .execute()
                .count) ?? 0

            var likedByMe = false
            if let myId {
                struct LikeRow: Decodable { let user_id: UUID }
                let mine: [LikeRow] = (try? await client.from("likes")
                    .select("user_id")
                    .eq("post_id", value: row.id)
                    .eq("user_id", value: myId)
                    .limit(1)
                    .execute()
                    .value) ?? []
                likedByMe = !mine.isEmpty
            }

            posts.append(DBPost(
                id: row.id, userId: row.user_id, username: profile.username, avatarUrl: profile.avatar_url,
                modelo: row.modelo, raridade: row.raridade, valorEstimadoUsd: row.valor_estimado_usd,
                fotoUrl: row.foto_url, caption: row.caption, createdAt: row.created_at,
                likeCount: likeCount, commentCount: commentCount, likedByMe: likedByMe
            ))
        }
        return posts
    }
```

Nota pro implementador: `.select(..., head: true, count: .exact)` retornando `.count` já foi confirmado funcionando no supabase-swift instalado (usado em `followCounts`, Follow feature desta mesma sessão) — reuse o mesmo padrão. Se a API real divergir um pouco, ajuste mantendo o comportamento (contar linhas sem baixar todas).

- [ ] **Step 3: `toggleLike`**

```swift
    static func toggleLike(postId: UUID) async throws -> Bool {
        try ensureSignedIn()
        guard let myId = client.auth.currentSession?.user.id else { throw SupabaseError.notSignedIn }

        struct LikeRow: Decodable { let user_id: UUID }
        let existing: [LikeRow] = try await client.from("likes")
            .select("user_id")
            .eq("post_id", value: postId)
            .eq("user_id", value: myId)
            .limit(1)
            .execute()
            .value

        if existing.isEmpty {
            struct NewLike: Encodable { let post_id: UUID; let user_id: UUID }
            try await client.from("likes")
                .insert(NewLike(post_id: postId, user_id: myId))
                .execute()
            return true
        } else {
            try await client.from("likes")
                .delete()
                .eq("post_id", value: postId)
                .eq("user_id", value: myId)
                .execute()
            return false
        }
    }
```

- [ ] **Step 4: `fetchComments` e `addComment`**

```swift
    static func fetchComments(postId: UUID) async throws -> [DBComment] {
        try ensureSignedIn()
        struct CommentRow: Decodable {
            let id: UUID
            let post_id: UUID
            let user_id: UUID
            let text: String
            let created_at: Date
        }
        let rows: [CommentRow] = try await client.from("comments")
            .select()
            .eq("post_id", value: postId)
            .order("created_at", ascending: true)
            .execute()
            .value

        var comments: [DBComment] = []
        for row in rows {
            struct ProfileRow: Decodable { let username: String }
            guard let profile: ProfileRow = try? await client.from("profiles")
                .select("username")
                .eq("id", value: row.user_id)
                .single()
                .execute()
                .value
            else { continue }
            comments.append(DBComment(id: row.id, postId: row.post_id, userId: row.user_id, username: profile.username, text: row.text, createdAt: row.created_at))
        }
        return comments
    }

    static func addComment(postId: UUID, text: String) async throws {
        try ensureSignedIn()
        guard let myId = client.auth.currentSession?.user.id else { throw SupabaseError.notSignedIn }
        struct NewComment: Encodable {
            let post_id: UUID
            let user_id: UUID
            let text: String
        }
        try await client.from("comments")
            .insert(NewComment(post_id: postId, user_id: myId, text: text))
            .execute()
    }
```

- [ ] **Step 5: Build** — `xcodebuild -project "Spot It/Spot It.xcodeproj" -scheme "Spotted" -destination "generic/platform=iOS Simulator" build` → `** BUILD SUCCEEDED **`, confirmar `SupabaseService.swift` recompilado.
- [ ] **Step 6: Commit**

```bash
git add "Spot It/Spot It/Services/SupabaseService.swift"
git commit -m "feat: SupabaseService ganha métodos de Feed (posts/likes/comments)

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01M7VA3TbyjrvN1NFVSqxW8Q"
```

---

### Task 4: `FeedView` + `FeedPostCard` com dado real

**Files:**
- Modify: `Spot It/Spot It/Views/FeedView.swift`
- Modify: `Spot It/Spot It/Views/FeedPostCard.swift`

**Interfaces:**
- Consumes: `fetchFeedPosts()`, `toggleLike(postId:)` (Task 3), `DBPost` (Task 2), `SupabaseService.isFollowing`/`follow`/`unfollow` (já existem, da frente de Follow desta sessão)

- [ ] **Step 1: `FeedView` carrega posts reais.** Trocar `private let posts: [FeedPost] = []` por `@State private var posts: [DBPost] = []`, adicionar `.task { await load() }` + `.refreshable { await load() }` com:

```swift
    private func load() async {
        posts = (try? await SupabaseService.fetchFeedPosts()) ?? []
    }
```

- [ ] **Step 2: `FeedPostCard` recebe `DBPost`.** Trocar `let post: FeedPost` por `let post: DBPost`. Ajustes:
  - `header`: usa `post.username`/`post.avatarUrl` (derive iniciais/cor igual ao padrão já usado em `SearchableUser` — reuse a mesma lógica de hash determinístico se for fácil, ou copie o essencial sem duplicarademais). `NavigationLink` pro `UserProfileView` passa `userId: post.userId` (real, não mais `nil` — este era um dos 4 call sites mock sinalizados na revisão do Follow, agora tem dado real disponível).
  - `photo`: `AsyncImage(url: URL(string: post.fotoUrl))` no lugar do `LinearGradient` de `photoGradient` (que não existe mais em `DBPost`), com fallback visual simples em caso de falha de load (mesmo padrão já usado em `ProfileView.photosGrid`).
  - `valueChip`: usa `post.valorEstimadoUsd` (já existe, sem mudança de lógica).
  - `actions` → curtir chama `SupabaseService.toggleLike(postId: post.id)` async, atualiza `@State private var isLiked`/`likeCount` locais com o retorno (não precisa recarregar o post inteiro).
  - Texto "Curtido por X e outros" vira `Text("\(likeCount) curtidas")` (esconder o `Text` inteiro se `likeCount == 0`).
  - Caption: `(Text(post.username).fontWeight(.semibold) + Text(" " + (post.caption ?? "")))");` — se `caption` for nil, mostrar só o username (sem texto extra estranho).
  - `FollowButton` privado (hoje `@State` local fake): trocar por uma versão que recebe `userId: UUID` e chama `SupabaseService.isFollowing`/`follow`/`unfollow` de verdade (mesmo padrão já usado em `UserProfileView.toggleFollow()` — pode extrair/copiar a lógica essencial, não precisa importar a view inteira). Só aparece quando `post.userId` não é o usuário logado (`SupabaseService.client.auth.currentSession?.user.id`).
  - `CarDetailPageView(item: WalletItem(feedPost: post))`: o init `WalletItem(feedPost:)` existente espera um `FeedPost` — ajuste para aceitar `DBPost` (renomeie o parâmetro/tipo do init em `FeedPost.swift`, ou crie um init equivalente `WalletItem(dbPost:)` ao lado; a struct `FeedPost` inteira e `Comment.sampleFor` podem ser deletados depois desta task se não sobrar nenhuma referência — confira com `grep -rn "FeedPost\b" "Spot It/Spot It"` antes de decidir).
  - `CommentsSheet(post: post, comments: $comments)`: a assinatura de `CommentsSheet` muda na Task 5 — se essa task rodar antes da 5, é esperado que o projeto não compile 100% ainda por causa disso; documente no relatório exatamente como a Task 4.

- [ ] **Step 3: Build.** Pode ter erro remanescente em `CommentsSheet.swift` (Task 5 ainda não rodou) — se for só isso, documente e prossiga; qualquer outro erro precisa ser corrigido antes de reportar DONE.
- [ ] **Step 4: Commit**

```bash
git add "Spot It/Spot It/Views/FeedView.swift" "Spot It/Spot It/Views/FeedPostCard.swift"
git commit -m "feat: FeedView e FeedPostCard usam posts reais

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01M7VA3TbyjrvN1NFVSqxW8Q"
```

---

### Task 5: `CommentsSheet` com dado real

**Files:**
- Modify: `Spot It/Spot It/Views/CommentsSheet.swift`

**Interfaces:**
- Consumes: `fetchComments(postId:)`, `addComment(postId:text:)` (Task 3), `DBComment` (Task 2), `DBPost` (Task 2)

- [ ] **Step 1: Ler o arquivo inteiro primeiro** pra entender a estrutura atual (recebe `post: FeedPost` e `comments: Binding<[Comment]>` hoje).
- [ ] **Step 2: Trocar a assinatura** pra receber `postId: UUID` (ou `post: DBPost` se for mais direto pro resto da view usar dados do post) em vez de `FeedPost`, e carregar/gravar `[DBComment]` via `fetchComments`/`addComment` no lugar do `Binding<[Comment]>` em memória. Layout visual (avatar, texto, "curtir comentário" se existir) mantém o mesmo — só a fonte de dado muda. O campo `likeCount`/`isLiked` de `Comment` (curtir um comentário) não tem equivalente em `DBComment`/na spec — **fora de escopo curtir comentário individual**, remova essa UI se ela depender só disso, ou deixe como visual estático sem função se for mais barato que reestruturar o layout inteiro (documente a escolha no relatório).
- [ ] **Step 3: Ajustar o call site em `FeedPostCard.swift`** (Task 4 já deve ter feito a chamada, mas confirme que os parâmetros batem com a assinatura nova desta task).
- [ ] **Step 4: Build.** `xcodebuild -project "Spot It/Spot It.xcodeproj" -scheme "Spotted" -destination "generic/platform=iOS Simulator" build` → **agora sim** espera-se `** BUILD SUCCEEDED **` sem erro nenhum em lugar nenhum do projeto.
- [ ] **Step 5: Commit**

```bash
git add "Spot It/Spot It/Views/CommentsSheet.swift" "Spot It/Spot It/Views/FeedPostCard.swift"
git commit -m "feat: CommentsSheet usa comentários reais

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01M7VA3TbyjrvN1NFVSqxW8Q"
```

---

### Task 6: Publicar no Feed a partir da Wallet

**Files:**
- Modify: `Spot It/Spot It/Views/CarDetailPageView.swift`
- Modify: `Spot It/Spot It/Views/WalletAllView.swift`
- Modify: `Spot It/Spot It/Views/WalletSummaryView.swift`
- Modify: `Spot It/Spot It/Views/SetDetailView.swift`

**Interfaces:**
- Consumes: `createPost(walletItemId:modelo:raridade:valorEstimadoUsd:fotoUrl:caption:)` (Task 3)

- [ ] **Step 1: `CarDetailPageView` ganha `let canPublish: Bool = false`** no início do struct (valor padrão `false` — não quebra os call sites existentes que não passam esse parâmetro). Adicionar um `@State private var showPublishSheet = false` e um botão na toolbar (`ToolbarItem(placement: .topBarTrailing)`, ícone tipo `square.and.arrow.up.on.square` ou similar) visível só quando `canPublish == true`, que abre um sheet simples:

```swift
struct PublishToFeedSheet: View {
    let item: WalletItem
    @Environment(\.dismiss) private var dismiss
    @State private var caption = ""
    @State private var isPublishing = false
    @State private var errorMessage: String?

    var body: some View {
        NavigationStack {
            Form {
                Section("Legenda (opcional)") {
                    TextField("Conta a história desse flagra...", text: $caption, axis: .vertical)
                }
                if let errorMessage {
                    Text(errorMessage).foregroundStyle(.red)
                }
            }
            .navigationTitle("Publicar no Feed")
            .navigationBarTitleDisplayMode(.inline)
            .toolbar {
                ToolbarItem(placement: .cancellationAction) {
                    Button("Cancelar") { dismiss() }
                }
                ToolbarItem(placement: .confirmationAction) {
                    Button("Publicar") { Task { await publish() } }
                        .disabled(isPublishing)
                }
            }
        }
    }

    private func publish() async {
        isPublishing = true
        defer { isPublishing = false }
        do {
            try await SupabaseService.createPost(
                walletItemId: item.id, modelo: item.modelo, raridade: item.raridade,
                valorEstimadoUsd: item.valorEstimadoUsd, fotoUrl: item.fotoUrl,
                caption: caption.isEmpty ? nil : caption
            )
            dismiss()
        } catch {
            errorMessage = error.localizedDescription
        }
    }
}
```

(pode ficar no mesmo arquivo `CarDetailPageView.swift`, como `private struct`, ou em arquivo próprio — escolha o que for mais consistente com o resto do projeto, que já mistura os dois padrões)

- [ ] **Step 2: Passar `canPublish: true`** nos 3 call sites que abrem `CarDetailPageView` a partir de um item real da própria Wallet: `WalletAllView.swift:153`, `WalletSummaryView.swift:147`, `SetDetailView.swift:37`. Os outros call sites (`MapView.swift`, `NotificationsView.swift`, `FeedPostCard.swift`) continuam sem passar o parâmetro (usa o padrão `false`) — não é escopo desta task decidir se publicar-a-partir-do-mapa faz sentido.
- [ ] **Step 3: Build** — `xcodebuild -project "Spot It/Spot It.xcodeproj" -scheme "Spotted" -destination "generic/platform=iOS Simulator" build` → `** BUILD SUCCEEDED **`.
- [ ] **Step 4: Commit**

```bash
git add "Spot It/Spot It/Views/CarDetailPageView.swift" "Spot It/Spot It/Views/WalletAllView.swift" "Spot It/Spot It/Views/WalletSummaryView.swift" "Spot It/Spot It/Views/SetDetailView.swift"
git commit -m "feat: publicar item da Wallet no Feed

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01M7VA3TbyjrvN1NFVSqxW8Q"
```
